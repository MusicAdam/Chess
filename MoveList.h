#ifndef MOVELIST_H
#define MOVELIST_H
#include<iostream>
#include<vector>

/*
class MoveData;

class MoveList
{
    public:
       //invalidates
        MoveList();
        virtual ~MoveList();

        //invalidates
        void storemove(MoveData newMove);

        //Decide which moves to move to queue and which moves to ignore
        //validates
    protected:
    private:
       vector<MoveData> m_possibleMoves;


        bool m_isValid;
}
*/
#endif // MOVELIST_H
